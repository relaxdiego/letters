baseURL = "https://letters.maglana.com/"
title = "Letters to My Sons"

# Nothing here is clever on purpose. If the custom domain ever dies, change
# baseURL to "https://relaxdiego.github.io/letters/" and rebuild.

disableKinds = ["taxonomy", "term", "rss"]

[params]
  # Used for the social preview card on pages that have no text of their own,
  # such as the homepage.
  description = "Letters from Mark Maglana to his sons."
